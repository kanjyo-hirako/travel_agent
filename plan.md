# 智能旅游助手 - 新功能开发计划

## 一、体验优化（Bug 修复）

### 1.1 修复快捷问题标签点击无响应 ✅
- **现状**：Chat 页面的快捷问题标签点击后没有反应，`handleClickTag` 是空函数
- **目标**：点击标签后自动填入输入框并发送消息
- **优先级**：高

### 1.2 修复城市列表重复项 ✅
- **现状**：`Home.vue` 中 `allCities` 数组包含重复的"西安"
- **目标**：去重
- **优先级**：高

### 1.3 修复地图无法标记地点问题
- **现状**：MapView.vue 中 `dayMarkers` 无法正确存储和访问标记数据
- **原因**：
  1. `dayMarkers` 是普通对象，不是 Vue 响应式数据
  2. `dayMarkers` 的键是字符串类型，但访问时传入的是数字类型
- **解决方案**：
  1. 将 `dayMarkers` 改为 `ref({})` 响应式数据
  2. 统一使用 `String(dayIndex)` 作为键访问
- **涉及文件**：`MapView.vue`
- **优先级**：高

---

## 二、新功能

### 2.1 行程分享功能（方案四：海报图片 + 原生分享组合）
- **实现方式**：
  - 使用 `html2canvas` 将行程详情渲染为精美海报图片
  - 支持 Web Share API 的设备走原生分享（带图片）
  - 不支持的设备 fallback 到下载图片 + 复制文本摘要
- **涉及页面**：`Detail.vue`
- **新增依赖**：`html2canvas`
- **优先级**：高

### 2.2 多人/家庭出行模式
- **实现方式**：
  - 在 Home 页增加出行人数、出行类型（独自/情侣/亲子/朋友/团建）选择
  - 将参数传给后端，调整 LLM prompt 以生成针对性行程
  - 行程详情页展示人均预算
- **涉及页面**：`Home.vue`、`Detail.vue`、后端 prompt
- **优先级**：中

### 2.3 天气集成
- **实现方式**：
  - 接入免费天气 API（如 OpenWeatherMap / 和风天气）
  - 在行程规划时获取目的地天气预报
  - 将天气信息传给 LLM，让 AI 根据天气调整建议
  - 行程详情页顶部展示天气卡片
- **涉及页面**：`Home.vue`、`Detail.vue`、后端服务
- **优先级**：中

### 2.4 行程地图可视化 ✅
- **实现方式**：
  - 接入地图 SDK（高德地图 JS API / 腾讯地图）
  - 在行程详情页底部增加地图区域，标注每天的景点位置
  - 用不同颜色/图标区分不同天数的路线
- **涉及页面**：`Detail.vue`
- **新增依赖**：地图 SDK
- **优先级**：中

### 2.5 行程导航功能（方案 A：调起外部地图 App）
- **实现方式**：
  - 点击景点标记或卡片，调起高德/百度地图 App 进行导航
  - 使用 URL Scheme 传递目的地坐标和名称
  - 在景点详情中显示"导航"按钮
  - 无需用户登录，无需定位权限
- **涉及页面**：`Detail.vue`、`SpotItem` 组件
- **技术细节**：
  - 高德地图：`amapuri://route/plan/?dlat={lat}&dlon={lng}&dname={name}&dev=0&t=0`
  - 百度地图：`baidumap://map/direction?destination=name:{name}|latlng:{lat},{lng}&mode=driving`
  - 腾讯地图：`qqmap://map/routeplan?type=drive&to={name}&tocoord={lat},{lng}`
- **优点**：
  - 实现简单，用户体验好
  - 支持实时路况、语音导航
  - 不需要处理复杂的定位和语音问题
- **涉及依赖**：无（纯前端实现）
- **优先级**：中

### 2.6 行程导出为 PDF
- **实现方式**：
  - 使用 `html2pdf.js` 或 `jsPDF` 将行程详情转换为 PDF
  - 支持一键下载
- **涉及页面**：`Detail.vue`
- **新增依赖**：`html2pdf.js`
- **优先级**：低

### 2.7 行程对比功能
- **实现方式**：
  - 用户可以对同一城市保存多份行程方案
  - 提供并排对比视图，对比预算、景点、天数安排等
- **涉及页面**：新增对比页面或弹窗
- **优先级**：低

### 2.8 自定义行程编辑
- **实现方式**：
  - AI 生成行程后，用户可手动调整（增删景点、修改时间、替换餐厅等）
  - 编辑后保存为自定义版本
- **涉及页面**：`Detail.vue`（需要较大改造）
- **优先级**：低

### 2.9 暗色模式
- **实现方式**：
  - 使用 CSS 变量定义主题色
  - 在 Profile 页增加主题切换开关
  - 记忆用户偏好到 localStorage
- **涉及页面**：全局样式 + `Profile.vue`
- **优先级**：低

### 2.10 景点详情增强
- **实现方式**：
  - 点击景点卡片展示更丰富信息（图片、评分、开放时间、交通方式）
  - 可接入高德/百度 POI 查询 API
- **涉及页面**：`SpotItem` 组件、新增景点详情弹窗
- **优先级**：低

### 2.11 预算追踪
- **实现方式**：
  - 旅途中记录实际花费
  - 与 AI 预估预算做对比，展示超支/节省情况
  - 数据保存在 localStorage
- **涉及页面**：新增预算追踪页面或在 Detail 页扩展
- **优先级**：低

---

## 三、开发建议顺序

1. ~~**Bug 修复**：1.1、1.2（快速完成，改善基础体验）~~ ✅ 已完成
2. ~~**地图可视化**：2.4~~ ✅ 已完成
3. **行程分享**：2.1（用户需求明确，实现成本低）
4. **多人出行**：2.2（扩展核心功能，改动可控）
5. **天气集成**：2.3（提升行程质量，需要 API key）
6. **行程导航**：2.5（依赖地图可视化功能）
7. **其余功能**：根据实际需求逐步推进
